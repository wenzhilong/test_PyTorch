一般来说，监督学习中的目标函数由损失函数和正则化项组成。Objective = Loss + Regularization

PyTorch中的损失函数一般在训练模型的时候指定。
注意：PyTorch中内置的损失函数的参数是y_pred在前，y_true在后。
自定义损失函数需要接受两个张量y_pred、y_true作为输入参数，并输出一个标量作为损失函数值。

PyTorch中国的正则化项一般通过自定义的方式和损失函数一起添加作为目标函数。